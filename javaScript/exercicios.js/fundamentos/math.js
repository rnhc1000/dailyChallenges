const raio = 5.6;
const area = Math.PI * Math.pow(raio,2);
console.log(area.toFixed(2));

/*
* Math é um objeto a partir do qual se acessam as 
* funções pow e o valor de PI
*/
console.log(typeof Math); 
