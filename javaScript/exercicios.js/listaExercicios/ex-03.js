/**
 * 03) Crie uma função que recebe dois parâmetros, 
 * base e expoente, e retorne 
 * a base elevada ao expoente. 
 * Usar o método Math.pow(base, expoente)
 * retorna o resultado.
 */
 


 const resultado = function(b, e) {
     return Math.pow(b,e);
 }

 console.log(resultado(2, 16))