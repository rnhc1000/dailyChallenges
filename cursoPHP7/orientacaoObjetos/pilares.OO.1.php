<div class="titulo">Pilares OO #1</div>
<?php
/**
 * 1. Encapsulamento
 * 2. Herança
 * 3. Polimorfismo
 * 4. Abstração
 * 
 * Encapsulanento
 * Não é necessário entender o funcionamento 
 * interno de um objeto, assim para se ter acesso 
 * ao mesmo, é necessaŕio fazer uso apenas dos métodos
 * e atributos daquele objeto, bastando apenas ser necessaŕio
 * se entender como comunicar-se com o objeto, não sendo necessariamente
 * ter-se acesso a membros internos que não tenham sido 
 * declarados como publicos.
 * A cápsula é o objeto que por sua vez possui métodos e atributos
 * O encapsulamewnto é implementado através do uso 
 * dos modificadores de acesso
 * PUBLIC -> PROTECTED -> PRIVATE
 * 
 * PUBLIC:
 * Marca um membro como visível para todo código com acesso à classe
 * 
 * PROTECTED:
 * Membro visível apenas para a própria classe e transmitido por herança
 * 
 * PRIVATE:
 * Aquele menbro é visível apenas dentro da classe que o define
 * 
 * 
 */

 ?>
 <h3>Encapsulamento </h3>

 <h4>Modificadores de Acesso </h4>
 <h5>PUBLIC </h5>
 <h5>PROTECTED</h5>
 <h5>PRIVATE</h5>

